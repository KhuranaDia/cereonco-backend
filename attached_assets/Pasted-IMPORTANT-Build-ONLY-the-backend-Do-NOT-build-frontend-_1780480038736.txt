IMPORTANT:

Build ONLY the backend.

Do NOT build frontend code.
Do NOT generate React components.
Do NOT generate UI screens.
Do NOT generate frontend routes.
Do NOT create frontend architecture.

The frontend already exists and is being handled separately.

Your responsibility is ONLY:

* Backend APIs
* Database schema
* Authentication
* Business logic
* Documentation
* Swagger
* Postman collection

Build the CereOnco backend from scratch using:

* Node.js
* Express.js
* PostgreSQL
* JWT Authentication
* bcrypt
* CORS
* Swagger/OpenAPI
* Postman Collection

Create this structure:

backend/
├── package.json
├── server.js
├── .env.example
├── README.md
├── docs/
│   ├── PROJECT_STATUS.md
│   └── postman-collection.json
└── src/
├── app.js
├── config/
│   └── db.js
├── controllers/
├── routes/
├── middleware/
├── models/
├── utils/
└── database/
├── schema.sql
└── seed.sql

Build Phase 1 + Phase 2 together.

PHASE 1 — AUTH & USERS

Features:

* PostgreSQL connection
* Health check
* User registration
* User login
* User logout
* JWT middleware
* Get current user profile
* Update current user profile
* Password hashing with bcrypt
* CORS setup
* Error handling

Users table:

* id
* name
* email
* password_hash
* role
* bio
* location
* created_at
* updated_at

Roles:

* patient
* caregiver
* medical_professional
* admin

APIs:

GET /api/healthz

POST /api/auth/register

POST /api/auth/login

POST /api/auth/logout

GET /api/users/me

PATCH /api/users/me

PHASE 2 — POSTS

Create tables:

posts
likes
bookmarks

Posts table:

* id
* user_id
* content
* image_url
* created_at
* updated_at

Likes table:

* id
* user_id
* post_id
* created_at

Bookmarks table:

* id
* user_id
* post_id
* created_at

Features:

* Create post
* Feed
* Single post
* Update own post
* Delete own post
* Like post
* Unlike post
* Bookmark post
* Remove bookmark
* Like count
* Bookmark count

APIs:

POST /api/posts

GET /api/posts

GET /api/posts/:id

PATCH /api/posts/:id

DELETE /api/posts/:id

POST /api/posts/:id/like

DELETE /api/posts/:id/like

POST /api/posts/:id/bookmark

DELETE /api/posts/:id/bookmark

Response format:

Success:

{
"success": true,
"message": "...",
"data": {}
}

Error:

{
"success": false,
"message": "..."
}

Requirements:

* Swagger documentation at /api/docs
* Postman collection generation
* README with setup instructions
* PROJECT_STATUS.md
* Database schema file
* Seed file
* Validation for all request bodies
* Modular code structure
* Postman testing instructions

After completion:

1. Run migrations
2. Start server
3. Test all endpoints
4. Provide public API URL
5. Provide exact Postman testing steps
6. Stop and wait for approval before building Phase 3 (Roles Extension) and Phase 4 (Comments & Replies)
